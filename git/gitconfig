[user]
name = Alex Kern
email = alex@kern.io

[color]
diff = auto
status = auto
branch = auto

[core]
excludesfile = ~/.gitignore
	editor = nvim

[apply]
whitespace = nowarn

[github]
user = kern

[mergetool]
keepBackup = false

[push]
default = matching

[credential]
helper = osxkeychain
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
